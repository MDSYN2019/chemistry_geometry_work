"""Exercise 28: networkx Graph construction and basic queries."""

import networkx as nx


def build_triangle_graph() -> nx.Graph:
    g = nx.Graph()
    # TODO: add three nodes: "H", "O", "C"
    # TODO: add edges so the graph is a triangle H-O-C-H
    return g


def node_count(g: nx.Graph) -> int:
    # TODO: return number of nodes
    return 0


def edge_count(g: nx.Graph) -> int:
    # TODO: return number of edges
    return 0


def has_edge(g: nx.Graph, u: str, v: str) -> bool:
    # TODO: return True if edge (u, v) exists
    return False
